# 小说任务关系力导图

静态网页版的小说任务/角色/情节关系可视化，作者：叶嘉新。

## 使用
1. 双击 `index.html` 直接在浏览器打开，或放到任意静态托管服务（如 Vercel、GitHub Pages）。
2. 左侧筛选支线、类别、时间范围，可拖拽节点并点击查看详情。
3. 右侧时间线自动同步当前筛选结果，便于按幕回顾情节。

> 页面基于 D3 力导布局实现，可离线运行，无需编译或额外依赖。
